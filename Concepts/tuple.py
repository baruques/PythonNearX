tupla = (1, 2, 3, 3, 3, 4) # It's unmutable and ordered (indexing works). Only allows the same data type and allows duplicates
print(tupla[0]) # Accessing first element

print(tupla.count(3)) # Counts how many times 3 appears in the tuple
print(tupla)